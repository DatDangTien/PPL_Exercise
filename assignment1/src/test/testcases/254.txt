x: integer = 65;
        inc: function void(out n: integer, delta: integer) {
            n = n + delta;
            while (_xy >= 0) i = i + 1;
                a: boolean = true;
            do {
                b = !a;
            }
            while (b == false); 
            return _testreturn();
        }
        